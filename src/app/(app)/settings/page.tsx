import { Button, Card, Input } from "@/components/ui";
import { demoUser } from "@/lib/demo";
export default function SettingsPage() { return <div className="space-y-6"><div><h1 className="text-3xl font-bold">Settings</h1><p className="text-slate-400">Manage your account security settings.</p></div><Card><h2 className="font-semibold">Account</h2><p className="mt-2 text-slate-400">Signed in as {demoUser.name} ({demoUser.email})</p></Card><Card><h2 className="font-semibold">SIM cost</h2><p className="text-sm text-slate-400">Monthly SIM expense deducted from net pay</p><label className="mt-4 block text-sm">SIM cost BDT/month<Input type="number" defaultValue={0} className="mt-2 max-w-sm" /></label><Button className="mt-4">Save SIM cost</Button><p className="mt-3 text-xs text-slate-500">Deducted once per month from net pay. You can update this anytime; admin can also change it from your employee profile.</p></Card><Card><h2 className="font-semibold">Password</h2><div className="mt-4 grid gap-4 md:grid-cols-2"><Input type="password" placeholder="New password"/><Input type="password" placeholder="Confirm"/></div><Button className="mt-4">Update password</Button></Card></div>; }

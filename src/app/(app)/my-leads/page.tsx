import { AddLeadsModal } from "@/components/AddLeadsModal";
import { TierTable } from "@/components/TierTable";
import { Card } from "@/components/ui";
import { formatBDT, getLeadTier, getNextTier } from "@/lib/payroll";
export default function MyLeadsPage() { const leads = 0; const next = getNextTier(leads); return <div className="space-y-6"><div className="flex flex-wrap justify-between gap-4"><div><h1 className="text-3xl font-bold">My leads</h1><p className="text-slate-400">Use Add leads to log counts by source and day. Entries need admin approval before they count toward payroll and tiers.</p></div><AddLeadsModal /></div><Card className="grid gap-4 md:grid-cols-5"><div><p className="text-slate-400">Current month</p><p className="font-semibold">May 2026</p></div><div><p className="text-slate-400">Total leads</p><p className="font-semibold">0</p></div><div><p className="text-slate-400">This month earnings</p><p className="font-semibold">{formatBDT(0)}</p></div><div><p className="text-slate-400">Current tier</p><p className="font-semibold">{getLeadTier(leads).label}</p></div><div><p className="text-slate-400">Next tier</p><p className="font-semibold">{next ? `${next.label} · ${formatBDT(next.rate)}` : "Top tier"}</p></div></Card><Card><h2 className="mb-4 font-semibold">This month</h2><div className="rounded-2xl border border-dashed border-slate-700 p-8 text-center text-slate-400">No entries yet for this month. Click Add leads to add your first day.</div></Card><TierTable leads={leads}/></div>; }

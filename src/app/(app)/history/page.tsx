import { Card } from "@/components/ui";
import { formatBDT } from "@/lib/payroll";
export default function HistoryPage() { return <div className="space-y-6"><div><h1 className="text-3xl font-bold">Payroll History</h1><p className="text-slate-400">All your finalized monthly payroll runs.</p></div><div className="grid gap-4 md:grid-cols-2"><Card><p className="text-slate-400">Pension collected so far</p><p className="mt-2 text-2xl font-bold text-red-300">{formatBDT(0)}</p></Card><Card><p className="text-slate-400">Net paid so far</p><p className="mt-2 text-2xl font-bold text-green-300">{formatBDT(0)}</p></Card></div><div className="overflow-auto rounded-2xl border border-slate-800"><table className="w-full min-w-[800px] text-sm"><thead className="bg-navy-800 text-slate-300"><tr>{["Month","Base","Sales / leads","Commission","Lead cost","SIM cost","Pension","Net","Status"].map(h => <th key={h} className="p-3 text-left">{h}</th>)}</tr></thead><tbody><tr><td colSpan={9} className="p-8 text-center text-slate-400">No finalized payroll runs yet.</td></tr></tbody></table></div></div>; }

import Link from "next/link";
import { BarChart3, ClipboardList, History, LayoutDashboard, Settings, Users, WalletCards } from "lucide-react";
const worker = [["Dashboard", "/dashboard", LayoutDashboard], ["My Leads", "/my-leads", BarChart3], ["Submissions", "/submissions", ClipboardList], ["History", "/history", History], ["Settings", "/settings", Settings]] as const;
const admin = [["Admin", "/admin", LayoutDashboard], ["Submissions", "/admin/submissions", ClipboardList], ["Employees", "/admin/employees", Users], ["Payroll", "/admin/payroll", WalletCards]] as const;
export function AppSidebar({ role = "WORKER" }: { role?: string }) { const items = role === "ADMIN" ? admin : worker; return <aside className="fixed inset-y-0 left-0 hidden w-64 border-r border-slate-800 bg-navy-900 p-4 md:block"><Link href={role === "ADMIN" ? "/admin" : "/dashboard"} className="mb-8 flex items-center gap-3"><span className="grid size-11 place-items-center rounded-2xl bg-blue-600 font-black">AB</span><div><p className="font-bold">AFF Bird</p><p className="text-xs text-slate-400">Payroll</p></div></Link><nav className="space-y-2">{items.map(([label, href, Icon]) => <Link key={href} href={href} className="flex items-center gap-3 rounded-xl px-3 py-2 text-sm text-slate-300 hover:bg-blue-500/10 hover:text-white"><Icon size={17}/>{label}</Link>)}</nav></aside>; }

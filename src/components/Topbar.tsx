import { Moon } from "lucide-react";
export function Topbar({ user }: { user: { name?: string | null; role?: string | null } }) { return <header className="sticky top-0 z-10 flex h-16 items-center justify-between border-b border-slate-800 bg-navy-950/90 px-4 backdrop-blur md:px-8"><div className="md:hidden"><span className="rounded-xl bg-blue-600 px-3 py-2 font-black">AB</span></div><div className="ml-auto flex items-center gap-4"><Moon className="text-slate-400" size={18}/><div className="text-right"><p className="text-sm font-semibold">{user.name}</p><p className="text-xs uppercase text-slate-500">{user.role}</p></div><div className="grid size-10 place-items-center rounded-full bg-blue-600 font-semibold">{user.name?.slice(0,1) ?? "U"}</div></div></header>; }

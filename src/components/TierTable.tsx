import { LEAD_TIERS, getLeadTier, getNextTier, formatBDT } from "@/lib/payroll";
export function TierTable({ leads }: { leads: number }) { const current = getLeadTier(leads); const next = getNextTier(leads); return <div className="overflow-hidden rounded-2xl border border-slate-800"><table className="w-full text-sm"><thead className="bg-navy-800 text-slate-300"><tr><th className="p-3 text-left">Tier</th><th className="p-3 text-right">Rate</th><th className="p-3 text-right">Status</th></tr></thead><tbody>{LEAD_TIERS.map(t => <tr key={t.label} className={t === current ? "bg-blue-500/10" : "bg-navy-900"}><td className="p-3">{t.label}</td><td className="p-3 text-right">{formatBDT(t.rate)} per lead</td><td className="p-3 text-right text-xs text-slate-400">{t === current ? "Current" : t === next ? "Next" : ""}</td></tr>)}</tbody></table></div>; }

import { StatusBadge } from "@/components/StatusBadge";
export type SubmissionRow = { id: string; day: string; month: string; source: string; leads: number; status: string; submitted: string; worker?: string };
export function SubmissionsTable({ rows, admin = false }: { rows: SubmissionRow[]; admin?: boolean }) { if (!rows.length) return <div className="rounded-2xl border border-dashed border-slate-700 p-8 text-center text-slate-400">No lead submissions yet.</div>; return <div className="overflow-auto rounded-2xl border border-slate-800"><table className="w-full min-w-[720px] text-sm"><thead className="bg-navy-800 text-slate-300"><tr>{admin && <th className="p-3 text-left">Worker</th>}<th className="p-3 text-left">Day</th><th className="p-3 text-left">Month</th><th className="p-3 text-left">Source</th><th className="p-3 text-right">Leads</th><th className="p-3 text-left">Status</th><th className="p-3 text-left">Submitted</th>{admin && <th className="p-3 text-left">Actions</th>}</tr></thead><tbody>{rows.map(r => <tr key={r.id} className="border-t border-slate-800 bg-navy-900">{admin && <td className="p-3">{r.worker}</td>}<td className="p-3">{r.day}</td><td className="p-3">{r.month}</td><td className="p-3">{r.source}</td><td className="p-3 text-right">{r.leads}</td><td className="p-3"><StatusBadge status={r.status}/></td><td className="p-3 text-slate-400">{r.submitted}</td>{admin && <td className="p-3 text-blue-300">Approve · Reject</td>}</tr>)}</tbody></table></div>; }
